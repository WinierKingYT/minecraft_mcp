id: pool.acquire
version: 1
milestone: V1.1
summary: Havuzdan bir runtime edinir.
status: planned

exposure:
  developer_tool: null
  debug_tool: pool_acquire
  bridge_operation: pool.acquire
  dsl_step: null

risk:
  effect: mutation
  scope: disposable_runtime
  reversibility: runtime_discard
  approval: none
  exposure: agent_visible
  cost: low
  level: R2

contracts:
  input: schemas/pool-acquire.input.json
  output: schemas/pool-acquire.output.json

limits:
  timeout_ms: 10000
  requires_idempotency: true

errors:
  - SUPERVISOR_UNAVAILABLE

tests:
  unit: [UT-POOL-ACQUIRE-001]
  contract: [CT-POOL-ACQUIRE-001]
