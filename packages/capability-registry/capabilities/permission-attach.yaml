id: permission.attach
version: 1
milestone: V1.1
summary: Test actor'una geçici bir izin atar.
status: planned

exposure:
  developer_tool: null
  debug_tool: permission_attach
  bridge_operation: permission.attach
  dsl_step: null

risk:
  effect: mutation
  scope: disposable_runtime
  reversibility: runtime_discard
  approval: none
  exposure: agent_visible
  cost: low
  level: R2

contracts:
  input: schemas/permission-attach.input.json
  output: schemas/permission-attach.output.json

limits:
  timeout_ms: 10000
  requires_idempotency: true

errors:
  - SUPERVISOR_UNAVAILABLE
  - PERMISSION_PROVIDER_UNSUPPORTED
  - TOOL_INPUT_INVALID

tests:
  unit: [UT-PERMISSION-ATTACH-001]
  contract: [CT-PERMISSION-ATTACH-001]
