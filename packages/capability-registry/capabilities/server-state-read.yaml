id: server.state.read
version: 1
milestone: M0
summary: Paper sürümü, build, tick sayacı, TPS/MSPT ve online oyuncu sayısını okur.
status: planned

exposure:
  developer_tool: null
  debug_tool: minecraft_server_get
  bridge_operation: server.get_state
  dsl_step: null

risk:
  effect: read
  scope: disposable_runtime
  reversibility: reversible
  approval: none
  exposure: agent_visible
  cost: low
  level: R0

contracts:
  input: schemas/server-state-read.input.json
  output: schemas/server-state-read.output.json

limits:
  timeout_ms: 2000

requires:
  runtime_state: [READY]

errors:
  - RUNTIME_NOT_RUNNING
  - BRIDGE_UNAVAILABLE
  - BRIDGE_BUSY
  - BRIDGE_TIMEOUT
  - CAPABILITY_UNAVAILABLE

tests:
  contract: [CT-SERVER-STATE-001]
  integration: [IT-SERVER-STATE-001]
